{
  "name": "@gliluaume/deco-router",
  "version": "1.0.3",
  "description": "decorator for routing in an express context",
  "main": "dist/index.js",
  "keywords": [
    "node",
    "typescript",
    "decorator",
    "express",
    "route",
    "routing"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/gliluaume/deco-router"
  },
  "files": [
    "dist/index.*"
  ],
  "scripts": {
    "build": "tsc --declaration",
    "serve": "nodemon dist/test-cases/test-app/server.js",
    "nopretest": "npm run build-ts",
    "test": "jest --detectOpenHandles --forceExit",
    "test:w": "jest --watch --detectOpenHandles --forceExit",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "lint:fix": "tslint -c tslint.json -p tsconfig.json --fix",
    "watch": "tsc -w"
  },
  "author": "gliluaume",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "reflect-metadata": "^0.1.13",
    "typescript": "^3.3.4000"
  },
  "devDependencies": {
    "@types/body-parser": "^1.17.0",
    "@types/compression": "0.0.36",
    "@types/express": "^4.16.1",
    "@types/jest": "^24.0.11",
    "jest": "^24.5.0",
    "nodemon": "^1.18.10",
    "supertest": "^4.0.2",
    "ts-jest": "^24.0.0",
    "ts-node": "^8.0.3",
    "tslint": "^5.14.0"
  }
}